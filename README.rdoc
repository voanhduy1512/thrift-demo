Support you already install Hbase

Install thrift with bundle with fail on Mac, so install it by manual:

  gem install thrift -- --with-cppflags='-D_FORTIFY_SOURCE=0'

install customer massive_record gem

  git clone https://github.com/voanhduy1512/massive_record
  cd massive_record
  bundle install
  rake install
  
clone this project

  git clone https://github.com/voanhduy1512/thrift-demo
  cd thrift-demo
  bundle install

modify HomeController to meet your need then: 
  rails s
